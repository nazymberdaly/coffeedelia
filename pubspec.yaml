name: coffeedelia
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  api_client:
    path: packages/api_client
  bloc: ^8.1.4
  cached_network_image: ^3.4.1
  coffee_domain:
    path: packages/coffee_domain
  coffee_repository:
    path: packages/coffee_repository  
  coffeedelia_ui:
    path: packages/coffeedelia_ui    
  db_client:
    path: packages/db_client  
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.2
  sqflite: ^2.4.1
  

dev_dependencies:
  bloc_test: ^9.1.7
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
